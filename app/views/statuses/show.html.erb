<div class="row">
	<div class="span12">
		<div class="status-index">
			<fieldset class="well">
		        <legend class="treebook-header">View Status</legend>			
				<strong>Name:</strong>
				<strong>
				  <% if !@status || !@status.user %>
				    Unknown
				  <% else %>
				    <%= @status.user.fullname %>
				  <% end %>
				</strong>

				<p>
			  		<strong>Content:</strong>
			  		<%= @status.content %>
				</p>			
			</fieldset>
			<div align="center">
				<div class="btn-group" > 
					<%= link_to 'Edit', edit_status_path(@status), :class => 'btn btn-primary'  %>
				</div>
				<div class="btn-group" > 	
					<%= link_to 'Cancel', statuses_path, :class => 'btn btn-primary'  %>
				</div>
			</div>
		</div>
	</div>
</div>