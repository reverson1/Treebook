<div class="row">
  <div class="span12">
    <div class="leaf-index" style="padding:0;">
		  <table width="100%" style="height:100%; background-color:#FFFFFF;">
	      <thead>
	        <tr>
	          <th align="center"  class="leaf header">
	            Edit Leaf
	          </th>
	        </tr>
	      </thead>
	      <tbody>
	      		<tr>	      		
	      		<td width="100%" height="100%" style="padding-top:20px;">
	      			<div align="center">
							<%= simple_form_for(@leaf, html: {class: "form-vertical"}) do |f| %>
								<% if @leaf.errors.any? %>
									<div id="error_explanation">
									  <h2><%= pluralize(@leaf.errors.count, "error") %> prohibited this leaf from being saved:</h2>
									  <ul>
									  <% @leaf.errors.full_messages.each do |msg| %>
									    <li><%= msg %></li>
									  <% end %>
									  </ul>	
									</div>
								<% end %>

						  	<%= f.input :content, :input_html => {:rows => 15, :style => 'width: 90%'}, :autofocus => true, label: false, placeholder:'Remember, stay leafy!' %>

					    	<%= f.submit 'Replant Leaf', :class => 'btn btn-primary' %>
								<%= link_to "Cancel", :back, :class => 'btn btn-primary' %>		
							<% end %>	
							</div>
							
						</td>
					</tr>
				</tbody>
			</table>
    </div>
  </div>
</div>    