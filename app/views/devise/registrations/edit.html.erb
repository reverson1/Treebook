<div class="row">
  <div class="span9, well">
    
    <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: {class:"form-horizontal", :method => :put}) do |f| %>
      <%= devise_error_messages! %>
      <fieldset>
        <legend>Edit <%= resource_name.to_s.humanize %></legend>
          <%= f.input :email, :autofocus => true %>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
          <% end %>                  

          <%= f.input :password, :label => "New Password", :autocomplete => "off", :hint => "Leave blank if you don't want to change it" %>
          <%= f.input :password_confirmation %>
          <%= f.input :current_password, :hint => "Enter your current password to confirm your changes" %>
        
          <div align='center'><%= f.button :submit, "Update", :class => 'btn btn-primary' %></div>
      </fieldset>
    <% end %>
  </div>
  <div class="span3, well"  align="center">
        <legend>Cancel my account</legend>
        <div class="btn-group" >             
          <%= link_to 'Cancel', registration_path(resource_name), :data =>  {:confirm => "Are you sure?"} , :method => :delete, :class => 'btn btn-primary' %>
        </div>
  </div>
</div>
